(executables
 ((names (obx wallet crowdsale))
  (public_names (obx wallet crowdsale))
  ;; (modules (util crowdsale))
  (preprocess (pps (ppx_sexp_conv)))
  (libraries (base stdio cmdliner bip39 toml ptime.clock.os ocplib-json-typed hex ctypes.foreign sodium blockexplorer-lwt bitcoin ledgerwallet))))
