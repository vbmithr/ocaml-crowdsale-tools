(executables
 ((names (wallet crowdsale obx))
  (public_names (wallet crowdsale obx))
  (preprocess (pps (ppx_sexp_conv)))
  (libraries (base stdio cmdliner bip39 toml ptime.clock.os ocplib-json-typed hex sodium blockexplorer-lwt))))
