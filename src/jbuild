(executables
 ((names (obx wallet crowdsale))
  (public_names (obx wallet crowdsale))
  (preprocess (pps (ppx_sexp_conv)))
  (libraries (base stdio cmdliner bip39 ptime.clock.os ocplib-json-typed hex tweetnacl blake2 blockexplorer-lwt bitcoin ledgerwallet))))
