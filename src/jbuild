(executables
 ((names (wallet utxos_of_pkhashs))
  (preprocess (pps (ppx_sexp_conv)))
  (libraries (hex sodium libbitcoin blockexplorer.async))))

(install
 ((section bin)
  (files (wallet.exe utxos_of_pkhashs.exe))))
