(executables
 ((names (wallet crowdsale))
  (preprocess (pps (ppx_sexp_conv)))
  (libraries (hex sodium libbitcoin blockexplorer.async))))

(install
 ((section bin)
  (files (wallet.exe crowdsale.exe))))
