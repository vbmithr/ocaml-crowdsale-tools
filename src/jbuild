(executables
 ((names (wallet crowdsale obx tx_of_dynamo))
  (public_names (wallet crowdsale obx tx_of_dynamo))
  (preprocess (pps (ppx_sexp_conv)))
  (libraries (base stdio cmdliner toml ptime.clock.os ocplib-json-typed hex sodium libbitcoin blockexplorer.lwt))))
